# For local development (running server.js on localhost:5000):
VITE_API_URL=http://localhost:5000/api

# For Vercel deployment, leave VITE_API_URL unset entirely
# (or set it to /api) since frontend and backend share the same domain.
